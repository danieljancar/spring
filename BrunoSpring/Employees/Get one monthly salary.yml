info:
  name: Get one monthly salary
  type: http
  seq: 13

http:
  method: GET
  url: "{{domain}}/employees/3/salary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
