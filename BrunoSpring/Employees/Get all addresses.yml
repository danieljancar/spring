info:
  name: Get all addresses
  type: http
  seq: 10

http:
  method: GET
  url: "{{domain}}/addresses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
