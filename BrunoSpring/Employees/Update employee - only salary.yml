info:
  name: Update employee - only salary
  type: http
  seq: 10

http:
  method: PUT
  url: "{{domain}}/employees/1"
  body:
    type: json
    data: |-
      {
        "salary": 85000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
