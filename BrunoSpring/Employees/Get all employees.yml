info:
  name: Get all employees
  type: http
  seq: 1

http:
  method: GET
  url: "{{domain}}/employees"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
