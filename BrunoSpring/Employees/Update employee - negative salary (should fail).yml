info:
  name: Update employee - negative salary (should fail)
  type: http
  seq: 13

http:
  method: PUT
  url: "{{domain}}/employees/1"
  body:
    type: json
    data: |-
      {
        "salary": -5000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
