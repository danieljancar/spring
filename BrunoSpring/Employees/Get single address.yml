info:
  name: Get single address
  type: http
  seq: 11

http:
  method: GET
  url: "{{domain}}/addresses/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
