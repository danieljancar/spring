info:
  name: Create employee - without salary
  type: http
  seq: 21

http:
  method: POST
  url: "{{domain}}/employees"
  body:
    type: json
    data: |-
      {
        "firstName": "Katherine",
        "lastName": "Johnson"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
