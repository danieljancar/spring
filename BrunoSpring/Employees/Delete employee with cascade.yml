info:
  name: Delete employee with cascade
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{domain}}/employees/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
