info:
  name: Get single employee
  type: http
  seq: 8

http:
  method: GET
  url: "{{domain}}/employees/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
