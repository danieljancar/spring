info:
  name: Get single employee
  type: http
  seq: 8

http:
  method: GET
  url: "{{domain}}/employees/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
