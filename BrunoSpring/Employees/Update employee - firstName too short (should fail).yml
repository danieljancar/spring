info:
  name: Update employee - firstName too short (should fail)
  type: http
  seq: 14

http:
  method: PUT
  url: "{{domain}}/employees/1"
  body:
    type: json
    data: |-
      {
        "firstName": "Al"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
